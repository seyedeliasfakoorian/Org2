name: Config and Scan
on:
  push:
    branches:
      - main
run-name: ${{ github.actor }} is Configuring and Scanning 🚀
jobs:
  Fix-Heroku-Issues:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '14'  # Adjust the Node.js version as needed

      - name: Install Heroku CLI and GitHub CLI
        run: |
          sudo npm install -g heroku
          heroku plugins:install heroku-builds
          sudo apt-get install -y gh

      - name: Reduce Vulnerabilities
        run: yarn audit

      - name: Set Heroku API Key
        run: echo "HEROKU_API_KEY=${{ secrets.HEROKU_API_KEY }}" >> $GITHUB_ENV

      - name: Login and Access to Heroku Container
        run: |
          HEROKU_APP_NAME="website-breaker-demo"
          curl -n -X PATCH https://api.heroku.com/apps/$HEROKU_APP_NAME/config-vars \
            -d "{
                  \"VARIABLE_NAME\": \"HEROKU_API_KEY\",
                  \"VALUE\": \"$HEROKU_API_KEY\"
                }" \
            -H "Content-Type: application/json" \
            -H "Accept: application/vnd.heroku+json; version=3"

      - name: Create Pull Request
        run: |
          HEROKU_APP_NAME="website-breaker-demo"

          # Placeholder for logic to determine if Heroku needs to check logs
          # For example, you can use the Heroku API to check the status of the last deployment
          HEROKU_STATUS=$(curl -n -s https://api.heroku.com/apps/$HEROKU_APP_NAME/releases/latest \
                            -H "Accept: application/vnd.heroku+json; version=3" \
                            | jq -r '.status')
          echo "Heroku Status: $HEROKU_STATUS"

          if [ "$HEROKU_STATUS" == "failed" ]; then
            echo "❌ Heroku deployment failed. Creating a pull request for checking logs."

            # Create a pull request
            PR_TITLE="Fix Heroku Vulnerabilities"
            PR_BODY="This pull request was automatically generated to trigger Heroku to check logs.🚀\n🌎 [View Site](https://$HEROKU_APP_NAME.herokuapp.com/)"

            echo "Attempting to create a pull request..."
            gh pr create --title "$PR_TITLE" --body "$PR_BODY" --base main --head github-actions
            echo "Pull request created successfully."
          else
            echo "✅ Heroku deployment successful."
          fi
